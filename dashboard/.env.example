# Guardian MCP server (must be running in SSE/HTTP mode)
NEXT_PUBLIC_GUARDIAN_URL=http://localhost:8080

# Archestra API (used by the scan page to list available servers)
NEXT_PUBLIC_ARCHESTRA_API=http://localhost:9000
